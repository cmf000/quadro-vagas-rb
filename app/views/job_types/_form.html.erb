<%= form_with model: job_type, url: url, method: method do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= form_errors job_type, :name %>

  <%= f.label :active %>
  <%= f.checkbox :active %>
  
  <%= f.submit t('.save') %>
<% end %>