
<%= form_with model: job_type, url: url, method: method do |f| %>
  <%= f.label :name, "Nome" %>
  <%= f.text_field :name %>

  <%= f.label :active, "Ativo" %>
  <%= f.checkbox :active %>
  
  <%= f.submit "Salvar" %>
<% end %>